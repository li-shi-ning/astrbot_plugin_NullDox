# 更新日志

## v1.1.3

- 输出格式改为身份行、随机句子行、头像分段展示。
- 新增 pytest 覆盖多行输出格式。

## v1.1.2

- 当用户名称为空或等于 ID 时，身份部分改为 `ID:xxx`。
- 新增 pytest 覆盖名称等于 ID 和名称缺失的输出格式。

## v1.1.1

- 主动命令输出的 `用户:XXX` 优先使用 AIOCQHTTP 事件中的群名片/昵称。
- 通过 `@用户` 指定目标时，优先使用 @ 组件解析出的目标名称。
- 名称不可用时继续回退为用户 ID，保持输出格式稳定。
- 新增 pytest 覆盖发送者昵称和 @ 目标名称解析。

## v1.1.0

- 移除假开盒信息生成，不再输出手机号、IP、地址或地图。
- 改为输出 `用户:XXX(ID:xxx)[随机句子]` 和用户头像。
- 新增公共随机句子词库。
- 新增分群随机句子词库，触发时与公共词库合并后随机抽取。
- 删除旧地理位置数据和地图说明文档。
- 新增 pytest 覆盖随机句子词库合并、默认词库回退和输出文本格式。

## v1.0.4

- 可选接入腾讯位置服务：随机地区搜索 POI，并附加静态地图图片。
- 支持腾讯 WebService SN 校验：填写 SK 时自动签名，留空时直接调用。
- 地图接口失败时自动回退到本地随机地址。
