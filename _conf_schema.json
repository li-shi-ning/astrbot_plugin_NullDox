{
  "user_list_mode": {
    "description": "主动命令账号过滤模式",
    "type": "string",
    "options": [
      "whitelist",
      "blacklist",
      "none"
    ],
    "default": "none",
    "hint": "whitelist: 仅允许列表内账号使用 /盒；blacklist: 拒绝列表内账号使用 /盒；none: 不限制。"
  },
  "user_list": {
    "type": "list",
    "description": "主动命令账号白名单/黑名单列表",
    "default": [],
    "hint": "用于控制哪些账号可以使用 /盒 指令。请填写用户账号 ID（如 QQ 号）。",
    "items": {
      "type": "string"
    }
  },
  "group_list_mode": {
    "description": "退群监听群聊过滤模式",
    "type": "string",
    "options": [
      "whitelist",
      "blacklist",
      "none"
    ],
    "default": "none",
    "hint": "whitelist: 仅监听列表内群聊的退群事件；blacklist: 不监听列表内群聊；none: 不限制。"
  },
  "group_list": {
    "type": "list",
    "description": "退群监听群组白名单/黑名单列表",
    "default": [],
    "hint": "用于控制哪些群会触发退群监听逻辑。支持填写 AstrBot UMO（如 xxxxx:GroupMessage:123456）或纯群号（如 123456，会自动匹配）。可使用 /sid 查看当前会话的 UMO。",
    "items": {
      "type": "string"
    }
  },
  "sentence_library": {
    "type": "object",
    "description": "公共随机句子词库",
    "hint": "所有群聊都会使用这里的句子。命中分群词库时，会把公共词库和该群词库合并后随机抽取。",
    "items": {
      "global_sentences": {
        "type": "list",
        "description": "公共随机句子",
        "hint": "每次输出会从公共词库和当前群聊专属词库合并后的结果中随机抽取一句。",
        "default": [
          "你已被随机句子命中。",
          "资料检索失败，但头像看起来很有故事。",
          "今日鉴定结果：适合继续水群。",
          "系统提示：该用户暂未发现异常，建议多观察。"
        ],
        "items": {
          "type": "string"
        }
      }
    }
  },
  "group_sentence_rules": {
    "type": "template_list",
    "description": "分群随机句子词库",
    "hint": "为指定群号配置额外句子。该群触发时会和公共词库合并使用，而不是替换公共词库。",
    "default": [],
    "templates": {
      "group_sentence_rule": {
        "name": "群聊句子词库",
        "hint": "给一个群单独配置随机句子。",
        "display_item": "group_id",
        "hide_hint_in_list": true,
        "items": {
          "group_id": {
            "type": "string",
            "description": "群号",
            "hint": "填写 QQ 群号，例如 123456789。",
            "default": ""
          },
          "sentences": {
            "type": "list",
            "description": "该群额外随机句子",
            "hint": "这些句子只在对应群聊中生效，并会与公共词库合并后随机抽取。",
            "default": [],
            "items": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
